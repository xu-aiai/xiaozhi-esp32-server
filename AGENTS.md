# 协作约定

本文件用于约束本仓库内智能代理与用户的协作方式。

## 语言要求

- 与用户交流时，统一使用中文。
- 输出各类文档时，统一使用中文。
- 代码注释、设计说明、使用说明、排障说明等文字内容，默认使用中文。

## Git 提交要求

- 提交 Git 时，`commit message` 统一使用中文。

## 默认原则

- 如无明确特殊要求，所有面向用户的说明性内容优先使用中文。
- 如需输出英文内容，应当由用户明确提出。
